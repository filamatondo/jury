<form method="post" action="{{ path('liste_ami_delete', {'id': liste_ami.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste_ami.id) }}">
    <button class="btn">Delete</button>
</form>
