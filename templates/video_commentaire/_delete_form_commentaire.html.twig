<form method="post" action="{{ path('video_commentaire_delete', {'id': video_commentaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ video_commentaire.id) }}">
    <button class="btn">Delete</button>
</form>
