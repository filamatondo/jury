{% extends 'base.html.twig' %}

{% block title %}Calepin{% endblock %}

{% block body %}
   <div class="register-form" style="margin:105px; margin-left: 100px;">

    <table class="table">
        <tbody>
            
            <tr>
                <th>Date</th>
                <td>{{ calepin.date ? calepin.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ calepin.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ calepin.Prenom }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ calepin.contenu }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ calepin.telephone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ calepin.Email }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ calepin.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('calepin_index') }}">Retour à la liste</a>

    <a href="{{ path('calepin_edit', {'id': calepin.id}) }}">Editer</a>

    {{ include('calepin/_delete_form.html.twig') }}
{% endblock %}
