<form method="post" action="{{ path('apropo_delete', {'id': apropo.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ apropo.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
