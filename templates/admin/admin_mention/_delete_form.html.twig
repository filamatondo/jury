<form method="post" action="{{ path('mention_delete', {'id': mention.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mention.id) }}">
	<button class="btn btn-danger" style="margin-top:-3%; margin-left:93%">Supprimer</button>
</form>
