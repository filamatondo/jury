{% extends 'base.html.twig' %}

{% block title %}ListeAmi{% endblock %}

{% block body %}
    <h1>ListeAmi</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ liste_ami.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('liste_ami_index') }}">back to list</a>

    <a href="{{ path('liste_ami_edit', {'id': liste_ami.id}) }}">edit</a>

    {{ include('liste_ami/_delete_form.html.twig') }}
{% endblock %}
