<form method="post" action="{{ path('liste_ami_ajouter_delete', {'id': liste_ami_ajouter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste_ami_ajouter.id) }}">
    <button class="btn">Delete</button>
</form>
