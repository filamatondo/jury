{% extends 'base.html.twig' %}

{% block title %}ListeAmiAjouter{% endblock %}

{% block body %}
    <h1>ListeAmiAjouter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ liste_ami_ajouter.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('liste_ami_ajouter_index') }}">back to list</a>

    <a href="{{ path('liste_ami_ajouter_edit', {'id': liste_ami_ajouter.id}) }}">edit</a>

    {{ include('liste_ami_ajouter/_delete_form.html.twig') }}
{% endblock %}
