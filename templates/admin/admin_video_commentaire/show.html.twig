{% extends 'base_admin_commentaire.html.twig' %}

{% block title %}VideoCommentaire
{% endblock %}

{% block body %}
	<h1>VideoCommentaire</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ video_commentaire.id }}</td>
			</tr>
			<tr>
				<th>Contenu</th>
				<td>{{ video_commentaire.contenu }}</td>
			</tr>
			<tr>
				<th>Date</th>
				<td>{{ video_commentaire.date ? video_commentaire.date|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('video_commentaire_index') }}">back to list</a>

	<a href="{{ path('video_commentaire_edit', {'id': video_commentaire.id}) }}">edit</a>

	{{ include('video_commentaire/_delete_form.html.twig') }}
{% endblock %}
