<form method="post" action="{{ path('img_delete', {'id': img.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ img.id) }}">
	<button class="btn btn-danger">supprimer</button>
</form>
