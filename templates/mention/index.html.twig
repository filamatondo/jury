{% extends 'base_apropos.html.twig' %}

{% block title %}Mentions Légales
{% endblock %}

{% block body %}


	<div class="titre  " style=" margin:20%; font-size:20px; margin-top:-60%; ">


		{% for mention in mentions %}

			<h1 class="titre " style="margin-top:10%; margin-left:37%">Mention légalses</h1>


			<div class="allo" style="margin-left:10%; margin:5%">{{ mention.article }}</div>

			<div class="allo" style="margin-left:10%;margin:5%">{{ mention.articl }}</div>

			<div class="allo" style="margin-left:10%;margin:5%">{{ mention.articles }}</div>
			<div>
				<a class="btn btn-success " href="{{ path('mention_show', {'id': mention.id}) }}" style="margin-left:50%; margin-top:5%">Afficher</a>
				<a class="btn btn-primary" href="{{ path('mention_edit', {'id': mention.id}) }}" style="margin-left:85%; margin-top:-3.4%">Éditer</a>
			</td>

		{% else %}

			<td colspan="4">Aucun enregistrement trouvé</td>

		{% endfor %}
	</div>


	<a class="btn btn-info" href="{{ path('mention_new') }}" style="margin-top:-3%">Créer un nouveau</a>

</div>{% endblock %}
